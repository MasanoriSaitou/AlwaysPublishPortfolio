① PyInstaller をインストール
VSCode のターミナルで：
pip install pyinstaller

② プロジェクトフォルダへ移動
cd プロジェクトのフォルダ

③ EXE を作るコマンド
pyinstaller --noconfirm --onefile app.py


これで dist/app.exeが生成される。


↓
失敗
↓
バッチを作成し、バッチを起動して実行する方法とした。
